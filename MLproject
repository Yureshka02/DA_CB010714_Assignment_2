name: player_performance_predictor

entry_points:
  train:
    command: "python src/model_training.py --training_seasons='{training_seasons}' --test_season={test_season} --n_estimators={n_estimators} --random_state={random_state}"
    parameters:
      training_seasons: {type: string, default: "1996-1999", help: "Range of seasons for training (e.g., 1996-1999)"}
      test_season: {type: int, default: 2000, help: "The season to test predictions against"}
      n_estimators: {type: int, default: 100, help: "Number of estimators for RandomForestRegressor"}
      random_state: {type: int, default: 42, help: "Random state for reproducibility"}

  # The predict entry point is defined in the previous guide, no change needed unless you want to update its parameters.
  # Example:
  # predict:
  #   command: "python src/model_prediction.py --model_name='{model_name}' --model_stage='{model_stage}' --input_data_path='{input_data_path}' --output_data_path='{output_data_path}'"
  #   parameters:
  #     model_name: {type: string, default: "PlayerPerformanceModel", help: "Name of the model in MLflow Model Registry"}
  #     model_stage: {type: string, default: "Production", help: "Stage of the model (e.g., Staging, Production)"}
  #     input_data_path: {type: string, help: "Path to CSV with new player data to predict"}
  #     output_data_path: {type: string, default: "predictions.csv", help: "Path to save prediction results"}